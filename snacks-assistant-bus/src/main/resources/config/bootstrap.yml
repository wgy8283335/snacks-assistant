spring:
  application:
    name: snacks-assistant-bus
  cloud:
    config:
      discovery:
        enabled: true
        service-id: snacks-assistant-config
      profile: application   # snacks-assistant-bus-application.yml