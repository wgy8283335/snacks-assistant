spring:
  application:
    name: snacks-assistant-goods
  cloud:
    config:
      discovery:
        enabled: true
        service-id: snacks-assistant-config
      profile: application  # snacks-assistant-goods-application.yml