spring:
  application:
    name: snacks-assistant-zuul
  cloud:
    config:
      discovery:
        enabled: true
        service-id: snacks-assistant-config
      profile: application #snacks-assistant-zuul-application.yml