spring:
  application:
    name: snacks-assistant-consumer
  cloud:
    config:
      discovery:
        enabled: true
        service-id: snacks-assistant-config
      profile: application   # snacks-assistant-consumer-application.yml
server:
  port: 8083
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
## 刷新时，关闭安全验证
management:
  endpoint:
    refresh:
      enabled: true
    health:
      enabled: true
management:
  endpoints:
    web:
      exposure:
        include: refresh,health