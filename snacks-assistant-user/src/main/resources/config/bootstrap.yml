spring:
  application:
    name: snacks-assistant-user
  cloud:
    config:
      discovery:
        enabled: true
        service-id: snacks-assistant-config
      profile: application  # snacks-assistant-user-application.yml

server:
  port: 8081
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/